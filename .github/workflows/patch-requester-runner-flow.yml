name: Patch requester runner flow

on:
  push:
    paths:
      - 'scripts/patch_requester_runner_flow.py'
      - '.github/workflows/patch-requester-runner-flow.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch app flow
        run: python3 scripts/patch_requester_runner_flow.py
      - name: Commit app change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "No app changes"
          else
            git commit -m "Move navigation to runner and support remote requester location"
            git push
          fi
