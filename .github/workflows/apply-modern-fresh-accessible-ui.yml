name: Apply modern fresh accessible UI

on:
  push:
    branches: [main]
    paths:
      - scripts/apply_modern_fresh_accessible_ui.py
      - scripts/run_modern_fresh_accessible_ui.trigger

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/apply_modern_fresh_accessible_ui.py
      - name: Commit generated UI patch
        run: |
          if git diff --quiet; then
            echo "No changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Refresh UI for readability and accessibility [skip ci]"
          git push
