name: Apply Community safety privacy v1

on:
  workflow_dispatch:
  push:
    paths:
      - 'community-safety.js'
      - 'scripts/apply_community_safety_privacy_v1.py'
      - '.github/workflows/apply-community-safety-privacy-v1.yml'

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Wire Community safety layer
        run: python scripts/apply_community_safety_privacy_v1.py
      - name: Validate safety JavaScript
        run: node --check community-safety.js
      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "Nothing to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Enable Community safety and privacy layer [skip ci]"
          git push
