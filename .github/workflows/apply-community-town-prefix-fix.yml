name: Apply Community town prefix fix

on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/apply_community_town_prefix_fix.py'
      - '.github/workflows/apply-community-town-prefix-fix.yml'

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply municipality prefix fix
        run: python scripts/apply_community_town_prefix_fix.py
      - name: Validate Community JavaScript
        run: node --check community-routes.js
      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "Nothing to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add community-routes.js index.html
          git commit -m "Fix Community municipality autocomplete prefixes [skip ci]"
          git push
